<h1>Editing proposal</h1>

<% form_for([@site,@proposal]) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </p>
 
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @proposal %> |
<%= link_to 'Back', site_proposals_path %>