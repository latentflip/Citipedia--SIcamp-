<h1>Listing comments</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Title</th>
    <th>Content</th>
    <th>Proposal</th>
  </tr>

<% @comments.each do |comment| %>
  <tr>
    <td><%=h comment.name %></td>
    <td><%=h comment.title %></td>
    <td><%=h comment.content %></td>
    <td><%= link_to 'Show', site_comment_path(comment.site,comment) %></td>
    <td><%= link_to 'Edit', edit_site_comment_path(comment.site,comment) %></td>
    <td><%#= link_to 'Destroy', destroy_site_comment_path(comment.site,comment), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New comment', new_site_comment_path %>